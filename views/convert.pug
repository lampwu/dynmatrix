extends layout

block content
  h1= title
  p Welcome to #{title}
  form(ref='uploadForm'
      id='uploadForm' 
      action='/upload' 
      method='post' 
      encType="multipart/form-data")
        input(type="file" name="dyn")
        input(type="submit" value="Convert!")